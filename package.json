{
  "name": "muralla",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["client", "server", "muralla-dashboard"],
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "scripts": {
    "dev:frontend": "cd muralla-dashboard && npm run dev",
    "dev:backend": "cd server && npm run dev",
    "build": "cd muralla-dashboard && npm install && NODE_ENV=production npm run build",
    "install:all": "npm install && cd server && npm install && cd ../muralla-dashboard && npm install",
    "start:production": "cd server && NODE_ENV=production npm start"
  }
}
